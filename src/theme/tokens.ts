import { black, black_800, white } from './colors'

const tokens = {
  colors: {
    light: {
      'bg-default': white,
      'fg-default': black,
      'overlay-bg': black_800,
    },
    dark: {
      'bg-default': black,
      'fg-default': white,
      'overlay-bg': black_800,
    },
  },
}

export default tokens
